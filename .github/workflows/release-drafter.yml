name: Release Drafter

on:
  push:
    branches:
      - master
      - release/*
  pull_request:
    types: [opened, reopened, synchronize]
  workflow_dispatch:

jobs:
  release_drafter:
    uses: ./market-release-drafter-action.yml
