pipeline {
  agent {
    label 'linux'
  }

  options {
    buildDiscarder(logRotator(numToKeepStr: '60', artifactNumToKeepStr: '5'))
  }

  triggers {
    pollSCM 'H/15 * * * *'
  }

  stages {
    stage('build') {
      steps {
        script {
          currentBuild.description = "On ${env.NODE_NAME}"
          builtModules = false
          docker.build('build-portal-modules', '-f build/Dockerfile.python .').inside('-v /var/tools/maven-cache:/home/build/') {
            utils = load 'build/common/utils.groovy'
            utils.updatePortalDependenciesAsRangeVersion()
            maven cmd: "clean install -f pom.xml -pl '!AxonIvyPortal/portal-test,!AxonIvyPortal/portal-selenium-test'"
            builtModules = true
          }
          archiveArtifacts artifacts: '*/*/target/*.iar', allowEmptyArchive: true
        }
      }
    }

    stage('deploy') {
      steps {
        script {
          def buildPluginVersion = readMavenPom(file: 'AxonIvyPortal/portal-components/pom.xml').getProperties().getProperty("build.plugin.version")
          def engineUrl = 'http://' + utils.getJenkinsMasterDomain() + ':8000' + '/'
          docker.build('deploy-modules-to-engine', '-f build/Dockerfile .').inside('-v /var/tools/maven-cache:/home/build/') {
            if (builtModules) {
              def deploymentPom = 'AxonIvyPortal/portal-selenium-test/deployment-pom.xml'
              maven cmd: "clean package -f ${deploymentPom}"
              def deployZip = sh(returnStdout: true, script: "find \${PWD}/AxonIvyPortal/portal-selenium-test/target -name '*.zip' | head -1").trim()
              maven cmd: "com.axonivy.ivy.ci:project-build-plugin:${buildPluginVersion}:deploy-to-engine -f ${deploymentPom} -Divy.deploy.file=${deployZip} -Divy.deploy.server.id=engine-cockpit -Divy.deploy.engine.app=Portal -Divy.deploy.method=HTTP -Divy.deploy.engine.url=${engineUrl} -Divy.test.engine=MODIFY_EXISTING -Divy.deploy.test.users=true -Divy.deploy.engine.app.version=released"
            }
          }
        }
      }
    }
  }
}