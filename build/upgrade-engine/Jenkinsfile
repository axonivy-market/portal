pipeline {
  agent {
    label 'linux'
  }

  options {
    buildDiscarder(logRotator(numToKeepStr: '60', artifactNumToKeepStr: '5'))
  }

  parameters {
    choice(name: 'engine', choices: ['portal','performanceTest'], description: 'Which engine to be upgraded?')
  }

  stages {
    stage('backupDatabase') {
      steps {
        script {
          today = new Date().format('dd-MM-yyyy_HH-mm')
          config = [portal:[engine: '10', systemDatabase: 'AxonIvySystemDatabase-10'],
                    performanceTest:[engine: 'performance-10', systemDatabase: 'AxonIvySystemDatabase-performance-10']]
          engineParentDir = '/var/tools/ivy/'
          engineDir = engineParentDir + config[params.engine]['engine']
          databaseBackupDir = '/var/database-backups/'
          databaseName = config[params.engine]['systemDatabase']
          dbBackupFile = databaseBackupDir + databaseName + '-' + today + '.dump'

          sh """
            echo ====================Backup database====================
            cd ${engineDir}
            [ -d ${databaseBackupDir} ] ||  mkdir -p ${databaseBackupDir}
            docker exec -u postgres postgres-10 pg_dump -Fc ${databaseName} > ${dbBackupFile}
          """
        }
      }
    }
    
    stage('upgradeEngine') {
      steps {
        script {
          sh """
            echo ====================Upgrade engine====================
            cd ${engineDir}
            docker compose stop
            docker compose pull
            docker compose up -d
          """
        }
      }
    }
  }
}


