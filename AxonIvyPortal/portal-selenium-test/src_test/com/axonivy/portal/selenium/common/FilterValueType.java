package com.axonivy.portal.selenium.common;

public enum FilterValueType {
  TEXT, DATE, DATE_BETWEEN, NUMBER, NUMBER_BETWEEN, STATE_TYPE, CATEGORY_TYPE, CREATOR_TYPE, DATE_CURRENT, WITHIN, RESPONSIBLE_TYPE;
}
