pipeline {
  agent any
  
  options {
    buildDiscarder(logRotator(numToKeepStr: '60', artifactNumToKeepStr: '5'))
  }
  
  triggers {
    pollSCM '@hourly'
    cron '@midnight'
  }

  tools { 
      maven 'default' 
      jdk 'jdk11' 
  }

  stages {
    stage('build') {
      steps {
        script {
          bat 'echo 123'
          //bat 'mvn clean verify -f AxonIvyPortal/PortalStyle/pom.xml'
          maven cmd: 'clean verify -f A1213xonIvyPortal/PortalStyle/pom.xml'
        }
      }
    }

  }
}


